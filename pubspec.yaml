name: mini_project_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1
  
dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1      # ✅ move it here (same level as flutter)
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.10.3
  google_sign_in: ^7.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/waterfall.webp
    - assets/images/login.jpg
    - assets/images/google logo.png
